public class Main {
    public static void main(String[] args) {
        // Initialize the UserCart JFrame
        LoginScreen login = new LoginScreen();

        // Set frame properties
        login.setVisible(true);
        login.setDefaultCloseOperation(LoginScreen.EXIT_ON_CLOSE);
    }
}
